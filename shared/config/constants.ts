/**
 * Project Constants
 *
 * Centralized constants used across the application.
 */

/** Application name */
export const APP_NAME = "Individual Finance";

/** Application version */
export const APP_VERSION = "0.1.0";

/** Default currency code */
export const DEFAULT_CURRENCY = "USD";

/** Decimal precision for money values */
export const MONEY_DECIMAL_PRECISION = 2;

/** Maximum description length for transactions */
export const MAX_DESCRIPTION_LENGTH = 500;

/** Maximum note length for entries */
export const MAX_NOTE_LENGTH = 1000;

/** Default page size for pagination */
export const DEFAULT_PAGE_SIZE = 20;

/** Maximum page size for pagination */
export const MAX_PAGE_SIZE = 100;

/** Session timeout in seconds (7 days) */
export const SESSION_TIMEOUT_SECONDS = 7 * 24 * 60 * 60;

/** Minimum password length */
export const MIN_PASSWORD_LENGTH = 8;

/** Maximum group members */
export const MAX_GROUP_MEMBERS = 50;

/** Goal deadline maximum days in future */
export const GOAL_DEADLINE_MAX_DAYS = 3650; // 10 years
